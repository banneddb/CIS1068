class rectangle {
    int length;
    int width;
    int x;
    int y;

    public rectangle(int length, int width, int x, int y) {
        this.length = length;
        this.width = width;
        this.x = x;
        this.y = y;
    }

    public int increaseLength() {
        int length = getLength();
        int increasedAmount = 0;
        return length+increasedAmount;

    }
    public int getLength() {
        return length;
    }
    public int getArea(int length,int width) {
        length
    }
}